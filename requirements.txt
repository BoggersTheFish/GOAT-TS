# Phase 0 minimal: clone → pip install -r requirements.txt → something runs.
# Core: NebulaGraph client, LangChain, Hugging Face, PyTorch, pytest.
nebula3-python>=3.8.0,<4
langchain>=0.3.0
langchain-community>=0.3.0
langchain-huggingface>=0.1.2
transformers>=4.38.0
torch>=2.2.0
numpy>=1.24.0
pytest>=8.0.0
# Config and HTTP
pyyaml>=6.0.2
python-dotenv>=1.0.0
requests>=2.32.0
pydantic>=2.0.0
tqdm>=4.65.0
# Graph and simulation
networkx>=3.4.0
matplotlib>=3.7.0
# Vector index (external backend; embeddings in node metadata)
faiss-cpu>=1.7.4
# Optional for full stack (Spark, Redis, monitoring)
pyarrow>=14.0.0
pyspark>=3.5.0
redis>=5.0.0
prometheus-client>=0.21.0
sentence-transformers>=3.0.0
